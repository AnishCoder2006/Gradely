# Student Record Management frontend

The canonical project documentation is in the repository root: [README.md](../README.md).

Run the complete local stack from the repository root with:

```bash
docker compose up --build
```
